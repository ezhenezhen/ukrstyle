<p id="notice"><%= notice %></p>

<% @cart.line_items.each do |item| %>
    <li><%= item.product.name %></li>
    <% end %>


<%= button_to 'Очистить корзину', @cart,
              :method => :delete, :confirm => 'Вы уверены?' %>
